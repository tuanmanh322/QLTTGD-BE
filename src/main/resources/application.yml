server:
  port: 1234

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qltrungtamtoanhoc1
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: shaiya01

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
  servlet:
    multipart:
      max-file-size: 3MB
      max-request-size: 3MB
      file-size-threshold: 2KB
      enabled: true
logging:
  level:
    ROOT: ERROR
    com.da: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.jdbc.core.JdbcTemplate: DEBUG

application:
  cors:
    allowed-origins: '*'
    allowed-methods: '*'
    allowed-headers: '*'
    exposed-headers: 'Authorization'
    allow-credentials: true
    max-age: 3600
  authentication:
    jwt:
     secret-key: enter-secret-token-for-application
     token-validity-milliseconds: 86400000
     token-validity-in-seconds-for-remember-me: 2592000000



storage:
  location: "./uploads"
